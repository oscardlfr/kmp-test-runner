{
  "_comment": "Allowlist for parity.test.js sub-check 3 (README ↔ code drift). Each entry justifies a deliberate gap between what the parsers accept and what the README's `### Flag reference` table documents. The README table is curated for the parallel/coverage flow; subcommand-specific flags live in their `kmp-test <sub> --help` text instead. Adding a new flag to a parser without a corresponding README row should require either a doc update OR a deliberate allowlist entry with a category here.",
  "parsedButNotInReadme": [
    "--help",
    "--version",
    "--force",
    "--timeout",
    "--device",
    "--device-task",
    "--auto-retry",
    "--clear-data",
    "--capture-on-fail",
    "--capture-dir",
    "--flavor",
    "--staged-only",
    "--show-modules-only",
    "--skip-app",
    "--verbose",
    "--list",
    "--list-only",
    "--platform",
    "--ignore-gradle-timeout",
    "--no-adb",
    "--skip-probe",
    "--no-cache",
    "--check",
    "--prefix",
    "--prerelease"
  ],
  "_categories": {
    "globalMeta": ["--help", "--version", "--force"],
    "androidSubcommand": ["--device", "--device-task", "--auto-retry", "--clear-data", "--capture-on-fail", "--capture-dir", "--flavor", "--skip-app", "--verbose", "--list", "--list-only"],
    "changedSubcommand": ["--staged-only", "--show-modules-only"],
    "benchmarkSubcommand": ["--platform", "--timeout", "--ignore-gradle-timeout"],
    "infoSubcommand": ["--no-adb"],
    "describeSubcommand": ["--skip-probe", "--no-cache"],
    "updateSubcommand": ["--check", "--prefix", "--prerelease"]
  },
  "readmeButNotParsed": [],
  "_skillRefComment": "Allowlist for parity.test.js sub-check 5 (SKILL flags-reference ↔ code drift). `.skills/kmp-test-runner/references/cli/flags-reference.md` is the canonical agent flag reference; it aims for near-complete coverage of the test-running flow flags. Each entry below is a parsed flag deliberately NOT documented there, with a category. The gate's value is catching a NEW parser flag that skips the agent reference — add a row to flags-reference.md OR a justified entry here.",
  "parsedButNotInSkillRef": [
    "--benchmark",
    "--check",
    "--prefix",
    "--prerelease",
    "--format",
    "--version"
  ],
  "_skillRefCategories": {
    "globalMetaOrAlias": ["--version", "--format"],
    "parallelBenchmarkPassthrough": ["--benchmark"],
    "updateSubcommand": ["--check", "--prefix", "--prerelease"]
  },
  "skillRefButNotParsed": []
}
