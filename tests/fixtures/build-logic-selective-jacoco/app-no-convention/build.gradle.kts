// Module that applies NO convention plugin. Like nowinandroid's many
// feature modules that just use kotlin("jvm")/kotlin("multiplatform")
// directly. Expected: coveragePlugin = null.
plugins {
    kotlin("jvm")
}
